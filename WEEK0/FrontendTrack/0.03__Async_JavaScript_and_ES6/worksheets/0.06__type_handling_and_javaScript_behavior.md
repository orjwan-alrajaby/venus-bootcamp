# 🧪 SECTION: Worksheet 6 — Type Handling & JavaScript Behavior

## 🎯 Goal

Practice:

* type conversion (coercion)
* working with `NaN`
* identifying empty values
* comparing values and objects
* understanding how JavaScript behaves in tricky cases

---

## ⚠️ Rules

* Use JavaScript only
* Do NOT use external libraries
* Add `console.log()` to test your answers
* Do NOT hardcode answers — write reusable functions

---

## 🧩 Challenge 1 — Convert String to Number

Create a function called `convertStringToNumber`.

It should:

* take a value as input
* convert it to a number using the unary `+` operator
* if the input is NOT a string, return an object with:

  * the original value
  * its type

```js
const convertStringToNumber = (input) => {
  // your code here
};

console.log(convertStringToNumber('25'));
console.log(convertStringToNumber(10));
```

👉 Expected output:

```js
25
{ value: 10, type: 'number' }
```

---

## 🧩 Challenge 2 — Check for NaN

Create a function called `checkNaN`.

It should:

* return `true` if the value is `NaN`
* return `false` otherwise

```js
const checkNaN = (value) => {
  // your code here
};

console.log(checkNaN(NaN));
console.log(checkNaN(10));
console.log(checkNaN('hello'));
```

👉 Expected output:

```js
true
false
false
```

---

## 🧩 Challenge 3 — Check Empty Values

Create a function called `isEmptyValue`.

It should return `true` if the value is:

* `undefined`
* `null`
* an empty string (`""`)

Otherwise, return `false`.

```js
const isEmptyValue = (value) => {
  // your code here
};

console.log(isEmptyValue(undefined));
console.log(isEmptyValue(null));
console.log(isEmptyValue(''));
console.log(isEmptyValue('hello'));
```

👉 Expected output:

```js
true
true
true
false
```

---

## 🧩 Challenge 4 — Compare Objects

Create a function called `compareObjects`.

It should:

* take two arguments
* if both are objects:

  * return `true` if they are equal
  * return `false` otherwise
* if either argument is NOT an object:

  * return an array of both inputs

```js
const compareObjects = (input1, input2) => {
  // your code here
};

console.log(compareObjects({ a: 1 }, { a: 1 }));
console.log(compareObjects({ a: 1 }, { a: 2 }));
console.log(compareObjects(10, { a: 1 }));
```

👉 Expected output:

```js
true
false
[10, { a: 1 }]
```

---

## 🧩 Challenge 5 — Complex Coercion

Create a function called `complexCoercion`.

It should:

* take a single argument
* check if the value is a primitive

If it is a primitive:

* if number → convert to string → return boolean
* if string → return boolean
* if `null` or `undefined` → return `false`

If it is NOT a primitive:

* return the value as-is

```js
const complexCoercion = (input) => {
  // your code here
};

console.log(complexCoercion(10));
console.log(complexCoercion('hello'));
console.log(complexCoercion(null));
console.log(complexCoercion({ name: 'Ahmad' }));
```

👉 Expected output:

```js
true
true
false
{ name: 'Ahmad' }
```

---

## 🧩 Challenge 6 — Predict the Output

Without running the code, write what each line will output:

```js
console.log('5' + 2);
console.log('5' - 2);
console.log(true + false);
console.log(null + 1);
console.log(undefined + 1);
```

Write your answers as comments:

```js
// '5' + 2 →
// '5' - 2 →
// true + false →
// null + 1 →
// undefined + 1 →
```

---

## 🧠 Reflections

* What is the difference between `==` and `===`?
* Why is `NaN` special?
* What is type coercion?
* When can type coercion cause bugs?
* Which challenge felt the most confusing and why?
