# 🔒 SECTION: Worksheet 5 — Closures & Scope

## 🎯 Goal

Practice:

* scope
* closures
* private variables
* `var` vs `let`
* functions inside loops
* recursion
* reading and explaining code

---

## ⚠️ Rules

* Use JavaScript only
* Do NOT use React
* Do NOT use external libraries
* Add `console.log()` examples to test your answers
* For explanation questions, write your answer as comments in the code file

---

## 🧩 Challenge 1 — Closure Counter

Create a function called `privateCounter`.

It should keep `count` private.

The returned object should have two methods:

* `increment()`
* `getCount()`

```js
const privateCounter = () => {
  // your code here
};

const counter = privateCounter();

counter.increment();
counter.increment();

console.log(counter.getCount());
```

👉 Expected output:

```js
2
```

---

## 🧩 Challenge 2 — Create Counter From Start

Create a function called `createCounter`.

It should take a starting number and return a function.

Every time the returned function is called, it should increase the counter by `1`.

```js
const createCounter = (start) => {
  // your code here
};

const counter = createCounter(5);

console.log(counter());
console.log(counter());
console.log(counter());
```

👉 Expected output:

```js
6
7
8
```

---

## 🧩 Challenge 3 — Power Function Closure

Create a function called `powerOf`.

It should take a base number.

It should return another function that takes an exponent.

```js
const powerOf = (base) => {
  // your code here
};

const powerOfTwo = powerOf(2);

console.log(powerOfTwo(3));
console.log(powerOfTwo(4));
```

👉 Expected output:

```js
8
16
```

---

## 🧩 Challenge 4 — Fibonacci Closure

Create a function called `createFibonacci`.

It should return a function.

Every time the returned function is called, it should return the next number in the Fibonacci sequence.

```js
const createFibonacci = () => {
  // your code here
};

const fib = createFibonacci();

console.log(fib());
console.log(fib());
console.log(fib());
console.log(fib());
console.log(fib());
console.log(fib());
```

👉 Expected output:

```js
0
1
1
2
3
5
```

---

## 🧩 Challenge 5 — Scope Bug with `var`

The current output is:

```js
5
5
5
5
5
```

Fix it so the output becomes:

```js
0
1
2
3
4
```

```js
for (var i = 0; i < 5; i++) {
  setTimeout(() => {
    console.log(i);
  }, 100);
}
```

---

## 🧩 Challenge 6 — Scope Bug with Array

The current output is:

```js
[0]
[1]
[2]
[3]
[4]
```

Fix it so the output becomes:

```js
[0, 1, 2, 3, 4]
```

```js
for (let i = 0; i < 5; i++) {
  let array = [];
  array.push(i);
  console.log(array);
}
```

---

## 🧩 Challenge 7 — Closure Loop Bug

The current output is:

```js
Current value of i is: 5
Current value of i is: 5
Current value of i is: 5
Current value of i is: 5
Current value of i is: 5
```

Fix it so the output becomes:

```js
Current value of i is: 0
Current value of i is: 1
Current value of i is: 2
Current value of i is: 3
Current value of i is: 4
```

```js
const functions = [];

for (var i = 0; i < 5; i++) {
  functions.push(() => {
    console.log('Current value of i is:', i);
  });
}

functions.forEach((func) => func());
```

---

## 🧩 Challenge 8 — Explain Scope Output

Read the two examples and explain why the output is different.

### Example 1

```js
function outer1() {
  var x = 10;

  var inner1 = function () {
    console.log(x);
  };

  inner1();
}

outer1();
```

👉 Output:

```js
10
```

Write your explanation:

```js
// Your explanation here:
```

---

### Example 2

```js
function outer2() {
  var x = 10;

  var inner2 = function () {
    var x = 20;
    console.log(x);
  };

  inner2();
}

outer2();
```

👉 Output:

```js
20
```

Write your explanation:

```js
// Your explanation here:
```

---

## 🧠 Reflections

* What is a closure?
* Why can closures “remember” values?
* Why does `var` behave differently from `let` inside loops?
* Why did the array reset in Challenge 6?
* Which challenge was hardest and why?
