# ⚙️ SECTION: Worksheet 7 — Higher Order Functions & `this`

## 🎯 Goal

Practice:

* higher order functions
* functions that return functions
* passing functions as arguments
* function wrappers
* `this` behavior
* scope inside nested functions

---

## ⚠️ Rules

* Use JavaScript only
* Do NOT use external libraries
* Add `console.log()` examples to test your answers
* For explanation questions, write your answers as comments

---

## 🧩 Challenge 1 — Repeat Function

Create a function called `repeatFunction`.

It should:

* take a function as an argument
* return a new function
* the returned function should take a number
* the input function should run that many times

```js
const sayHello = () => {
  console.log('Hello');
};

const repeatFunction = (func) => {
  // your code here
};

const repeatHello = repeatFunction(sayHello);

repeatHello(3);
```

👉 Expected output:

```js
Hello
Hello
Hello
```

---

## 🧩 Challenge 2 — Enhance a Function

Create a function called `withLogging`.

It should:

* take a function as an argument
* return a new function
* log `"Function started"` before running the original function
* log `"Function finished"` after running the original function

```js
const add = (a, b) => {
  return a + b;
};

const withLogging = (func) => {
  // your code here
};

const loggedAdd = withLogging(add);

console.log(loggedAdd(5, 10));
```

👉 Expected output:

```js
Function started
Function finished
15
```

---

## 🧩 Challenge 3 — Arrow Higher Order Function

Create a function called `arrowHOF`.

It should:

* take a function as an argument
* return a new function
* the returned function should accept the original function’s arguments
* then return another function that takes `times`
* when called, it should run the original function `times` times

```js
const exampleNormalFunc1 = (a, b, c) => {
  return a * (b + c);
};

const exampleNormalFunc2 = (x, y) => {
  return x * y;
};

const exampleNormalFunc3 = (string) => {
  return string + ' ' + string + ' ' + string + '!';
};

const arrowHOF = (normalFunc) => {
  // your code here
};

const hofNormalFunc1 = arrowHOF(exampleNormalFunc1);
const hofNormalFunc2 = arrowHOF(exampleNormalFunc2);
const hofNormalFunc3 = arrowHOF(exampleNormalFunc3);

hofNormalFunc1(3, 4, 5)(2);
hofNormalFunc2(20, 35)(4);
hofNormalFunc3('Meow')(1);
```

👉 Expected output:

```js
60
60

700
700
700
700

Meow Meow Meow!
```

---

## 🧩 Challenge 4 — Preserve `this`

Create a function called `preserveThis`.

It should:

* take a function as an argument
* return a new function
* preserve the correct `this` context when the function is called as an object method

```js
const user = {
  name: 'John',
  greet: function (greeting) {
    console.log(`${greeting}, ${this.name}!`);
  },
};

const preserveThis = (func) => {
  // your code here
};

user.preservedGreet = preserveThis(user.greet);

user.preservedGreet('Hello');
```

👉 Expected output:

```js
Hello, John!
```

---

## 🧩 Challenge 5 — Broken `this`

Explain why this code prints `undefined` or does not work as expected.

```js
const user = {
  name: 'Lina',
  greet: function () {
    console.log(this.name);
  },
};

const greetFunction = user.greet;

greetFunction();
```

Write your explanation:

```js
// Your explanation here:
```

---

## 🧩 Challenge 6 — Nested Scope

Read the two examples and explain why the output is different.

### Example 1

```js
function outer1() {
  var x = 10;

  var inner1 = function () {
    console.log(x);
  };

  inner1();
}

outer1();
```

👉 Output:

```js
10
```

Write your explanation:

```js
// Your explanation here:
```

---

### Example 2

```js
function outer2() {
  var x = 10;

  var inner2 = function () {
    var x = 20;
    console.log(x);
  };

  inner2();
}

outer2();
```

👉 Output:

```js
20
```

Write your explanation:

```js
// Your explanation here:
```

---

## 🧠 Reflections

* What is a higher order function?
* Why would we pass a function into another function?
* Why would a function return another function?
* What does `this` usually refer to?
* Why can `this` become confusing in JavaScript?
